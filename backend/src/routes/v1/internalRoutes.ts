import { Router } from 'express';

const router = Router();

// FEATURE INTEGRATION POINT
// Add internal (authenticated) routes here.
// Example: router.use('/books', bookRoutes);

export default router;
